# Worklog

## 2026-08-27 — issue #0

- Added a standalone scheduled Worker acceptance fixture whose cron handler
  emits the scheduled timestamp and cron expression without requiring any
  external service or Durable Object bindings.

- #0: Added the JavaScript Durable Object Worker shim and private build CLI. It translates the Cloudflare-style fetch, lifecycle, storage, SQL, alarm, and socket surface into the `verglas:do-worker@0.1.0` component world and records content-addressed build manifests.
- #0: Replaced the invented `env.storage` authoring API with the Cloudflare `cloudflare:workers` surface. Ported Durable Object IDs, namespaces, state, storage, SQL cursors, alarms, bindings, and waitUntil from the TypeScript implementation, adapting the transport to WIT storage, SQL-row, alarm, socket, and do-fetch verbs without client-side canonical envelopes. Updated the Wrangler subset, component build, gateway digest synchronization, and literal Cloudflare counter example; the old authoring-surface assertions were replaced as required by the frozen compatibility contract.
- #0: Rebuilt the counter component against the renamed `service` world and refreshed its gateway digest. The emitted component was checked with `jco wit` for worker/handler exports, storage/sockets/bindings imports, and no WASI imports.
- #171: Added strict Cloudflare Pipeline bindings to the JavaScript SDK. `env.<binding>.send(records)` validates JSON and the 5 MiB encoded request bound, then sends the fixed internal POST through `bindings.do-fetch` and resolves only for a 2xx durable acknowledgement; structured schema validation remains deferred to Pipeline processing.
- #0: Added the PP4 Durable Object Stream path: handler bindings call the WIT transactional `storage.stream-send` verb so records cannot append before source commit, while stateless Worker bindings retain the direct internal POST path. The Arrow SQL surface is absent from the SDK transport.
- #171: Updated the JavaScript builder to emit and update strict nested `artifacts.worker` and `artifacts.durable_object` descriptors with the exact SHA-256 component bytes. Retired top-level `component_digest` and `component_dir` gateway fields now fail closed, and build tests cover nested paths, byte verification, and the legacy rejection.
- #171: Added strict Wrangler service-binding parsing, duplicate-name rejection, build-manifest preservation, and direct `fetch` adaptation over the existing declared-binding ABI. The witnessed-red Catalog build first failed on the unknown `services` key; it now emits only `ICEBERG_COMMIT → verglas-runtime` and still has no WASI imports.
- #171: Removed documentation and transport comments for the deleted line-command and engine-envelope paths. The retained SDK describes only the Cloudflare Worker/Durable Object component transport.
- #179: Added the exact Wrangler `vectorize` manifest shape and a Cloudflare-style Worker binding with insert, upsert, query, query-by-id, get, delete, and describe methods over the existing declared `do-fetch` capability. Initialization failures now propagate through `blockConcurrencyWhile` so immutable builtin configuration fails activation instead of being hidden.
- #180: Added strict `graphs` manifest parsing and a fixed named Graph Worker binding for node and edge CRUD, bounded neighborhoods, deterministic shortest paths, and describe. The binding validates records and traversal ceilings before using the existing declared `do-fetch` capability.
- #181: Added strict `queries` manifest parsing, a fixed-name Query client binding, and stateless semantic JSX Dashboard components. The build preserves Query declarations, while Dashboard rendering resolves bounded Query results on demand and emits escaped server-side HTML and SVG.
- #182: Added strict `skills` manifest parsing and a fetch-only fixed-name Skill binding. The binding preserves the complete MCP 2026-07-28 HTTP envelope while preventing tenant Workers from selecting another Skill object.
- #182: Removed the special `skills` manifest binding because MCP skill bundles deploy as ordinary Workers. The component builder now embeds imported Markdown so a user-owned `SKILL.md` tree travels with the immutable Worker artifact.
- #0: Added strict Wrangler `triggers.crons` parsing and build-manifest preservation. Stateless components now expose Cloudflare-compatible `scheduled(controller, env, ctx)` handlers and await their `waitUntil` work before completing the host event.
- #182: Removed the abandoned MCP Worker build support, including Markdown embedding. Agent skills and their resource bundles remain outside the Worker SDK; the unsupported `skills` manifest key continues to fail closed.
- #0: Added Cloudflare's WebSocket upgrade extension to the guest `Response` compatibility layer so Durable Objects can pair `ctx.acceptWebSocket()` with status 101 even though the underlying WHATWG runtime accepts only 200–599. Regression coverage exercises the authored Cloudflare response shape.
- #0: Removed the final documentation references to the deleted general-purpose TypeScript client. The JavaScript Worker SDK now describes only its own structured-clone encoding and declared WIT capability boundary.
- #0: Move the JavaScript Worker authoring toolchain into verglas-sdk as the public `@verglas-org/worker-js` package. The package now carries its WIT and runtime assets, and the Verglas CLI resolves those assets through the package instead of maintaining a copy.
