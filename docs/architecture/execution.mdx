---
title: "Runtime roles"
description: "Keep the nine Worker/DO products separate from their shared runtime capabilities."
icon: "server"
---

Verglas has exactly nine products: Worker, Durable Object, Stream, Pipeline,
Sink, Catalog, Vectorize, Graph, and Query. Each prebuilt system product is a Worker/DO deployment. The
[Worker primitives platform](/docs/architecture/primitives-platform) is the
normative inventory and PP1–PP10 acceptance page. The composition described
here is a target where its status is not otherwise marked.

## Worker and Durable Object execution

A **Worker** is stateless, pool-executed code. It has no identity or storage;
its bindings are its authority. A **Durable Object** is a named stateful Worker
class with strictly serialized events and one embedded local Turso `0.7.2`
database under the self-hosted cell data root. Cell filesystem durability is
authoritative for the DO database; DO state has no clustering or replication.

A DO event commits KV/SQL/alarm/attachment state in one Turso transaction. The
host issues SQL `COMMIT`, completes an explicit WAL checkpoint, and then
releases event effects. Selected committed changes may also create transactional
outbox rows for a Stream. The Stream deduplicates `(object-id, event-sequence,
record-index)` identities, so a crash between Stream confirmation and the
delivered mark causes an idempotent resend.

A DO has no custom SQLite pager, alternate SQL provider, alternate mutation or
object-store authority, direct Iceberg offloader, or ordinary vector/graph mutation
domain. The Pipeline's stateless SQL transform is separate from DO storage.

## Prebuilt data products

- **Stream** is a prebuilt DO that durably appends ordered JSON. It does not
  offload, execute SQL, own consumer offsets, or call Iceberg/Catalog.
- **Pipeline** is a prebuilt Worker/DO that owns a Stream cursor, executes
  stateless SQL transforms, batches records, and retries Sink delivery. It
  contains no destination-specific Iceberg code.
- **Sink** is a prebuilt Worker/DO with an idempotency ledger. Its first adapter
  writes Parquet and commits Iceberg through a Catalog binding.
- **Catalog** is a prebuilt Worker/DO that exposes Iceberg REST and reuses the
  existing Verglas catalog and object-storage libraries. It owns metadata
  publication, not Pipeline cursors or Sink delivery state.
- **Vectorize** is a prebuilt Worker/DO whose named Turso database owns F32
  vectors, immutable dimensions and metric, metadata declarations, and bounded
  exact similarity search through Cloudflare-shaped binding methods.
- **Graph** is a prebuilt Worker/DO whose named Turso database owns relational
  nodes, directed edges, property declarations, and bounded indexed traversal.

## Shared runtime capabilities

`verglas-runtime` is the shared Wasmtime host for Worker and Durable Object
components. It owns the local Turso adapter, Foyer memory and disk tiers,
configured S3 origin access, and narrow privileged capabilities such as Iceberg
Catalog commits. Tenant components do not receive raw object-store, filesystem, or
network authority. See [architecture overview](/docs/architecture/overview) for
the repository boundary.
